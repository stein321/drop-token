# Drop Token

## Requirements 
 * Mongo db running on the default port  
  
## Build 
 `mvn clean install`
 
## Run 
   `java -jar target/drop-token.jar`
## Run with Devtools 
   `mvn spring-boot:run` 